"use strict";
/* tslint:disable */
/* eslint-disable */
/**
 * Faraday REST API
 * The [Faraday](https://faraday.ai) API makes it easy to predict customer behavior programmatically. See our [API change policy](https://faraday.ai/legal).
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@faraday.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ProjectsApi = void 0;
const runtime = require("../runtime");
/**
 *
 */
class ProjectsApi extends runtime.BaseAPI {
    /**
     * Archive a Project. A Project that a live Enrichment still refers to cannot be archived; the error names the Enrichments in the way.
     * Archive a project
     */
    archiveProjectRaw(requestParameters) {
        return __awaiter(this, void 0, void 0, function* () {
            if (requestParameters.projectId === null || requestParameters.projectId === undefined) {
                throw new runtime.RequiredError('projectId', 'Required parameter requestParameters.projectId was null or undefined when calling archiveProject.');
            }
            const queryParameters = {};
            const headerParameters = {};
            if (requestParameters.idempotencyKey !== undefined && requestParameters.idempotencyKey !== null) {
                headerParameters['Idempotency-Key'] = String(requestParameters.idempotencyKey);
            }
            if (this.configuration && this.configuration.accessToken) {
                const token = this.configuration.accessToken;
                const tokenString = yield token("bearer", []);
                if (tokenString) {
                    headerParameters["Authorization"] = `Bearer ${tokenString}`;
                }
            }
            const response = yield this.request({
                path: `/projects/{project_id}/archive`.replace(`{${"project_id"}}`, encodeURIComponent(String(requestParameters.projectId))),
                method: 'POST',
                headers: headerParameters,
                query: queryParameters,
            });
            return new runtime.JSONApiResponse(response);
        });
    }
    /**
     * Archive a Project. A Project that a live Enrichment still refers to cannot be archived; the error names the Enrichments in the way.
     * Archive a project
     */
    archiveProject(projectId, idempotencyKey) {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.archiveProjectRaw({ projectId: projectId, idempotencyKey: idempotencyKey });
            return yield response.value();
        });
    }
    /**
     * Declare the exact output columns you want, in the exact order you want them.  The response carries the resolved output schema and the usage ceiling straight away. No data is read and no person is sampled.
     * Create a project
     */
    createProjectRaw(requestParameters) {
        return __awaiter(this, void 0, void 0, function* () {
            if (requestParameters.project === null || requestParameters.project === undefined) {
                throw new runtime.RequiredError('project', 'Required parameter requestParameters.project was null or undefined when calling createProject.');
            }
            const queryParameters = {};
            const headerParameters = {};
            headerParameters['Content-Type'] = 'application/json';
            if (requestParameters.idempotencyKey !== undefined && requestParameters.idempotencyKey !== null) {
                headerParameters['Idempotency-Key'] = String(requestParameters.idempotencyKey);
            }
            if (this.configuration && this.configuration.accessToken) {
                const token = this.configuration.accessToken;
                const tokenString = yield token("bearer", []);
                if (tokenString) {
                    headerParameters["Authorization"] = `Bearer ${tokenString}`;
                }
            }
            const response = yield this.request({
                path: `/projects`,
                method: 'POST',
                headers: headerParameters,
                query: queryParameters,
                body: requestParameters.project,
            });
            return new runtime.JSONApiResponse(response);
        });
    }
    /**
     * Declare the exact output columns you want, in the exact order you want them.  The response carries the resolved output schema and the usage ceiling straight away. No data is read and no person is sampled.
     * Create a project
     */
    createProject(project, idempotencyKey) {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.createProjectRaw({ project: project, idempotencyKey: idempotencyKey });
            return yield response.value();
        });
    }
    /**
     * Delete a Project. A Project that a live Enrichment still refers to cannot be deleted; the error names the Enrichments in the way. Archive or delete those first.
     * Delete a project
     */
    deleteProjectRaw(requestParameters) {
        return __awaiter(this, void 0, void 0, function* () {
            if (requestParameters.projectId === null || requestParameters.projectId === undefined) {
                throw new runtime.RequiredError('projectId', 'Required parameter requestParameters.projectId was null or undefined when calling deleteProject.');
            }
            const queryParameters = {};
            const headerParameters = {};
            if (this.configuration && this.configuration.accessToken) {
                const token = this.configuration.accessToken;
                const tokenString = yield token("bearer", []);
                if (tokenString) {
                    headerParameters["Authorization"] = `Bearer ${tokenString}`;
                }
            }
            const response = yield this.request({
                path: `/projects/{project_id}`.replace(`{${"project_id"}}`, encodeURIComponent(String(requestParameters.projectId))),
                method: 'DELETE',
                headers: headerParameters,
                query: queryParameters,
            });
            return new runtime.VoidApiResponse(response);
        });
    }
    /**
     * Delete a Project. A Project that a live Enrichment still refers to cannot be deleted; the error names the Enrichments in the way. Archive or delete those first.
     * Delete a project
     */
    deleteProject(projectId) {
        return __awaiter(this, void 0, void 0, function* () {
            yield this.deleteProjectRaw({ projectId: projectId });
        });
    }
    /**
     * Retrieve a project
     */
    getProjectRaw(requestParameters) {
        return __awaiter(this, void 0, void 0, function* () {
            if (requestParameters.projectId === null || requestParameters.projectId === undefined) {
                throw new runtime.RequiredError('projectId', 'Required parameter requestParameters.projectId was null or undefined when calling getProject.');
            }
            const queryParameters = {};
            const headerParameters = {};
            if (this.configuration && this.configuration.accessToken) {
                const token = this.configuration.accessToken;
                const tokenString = yield token("bearer", []);
                if (tokenString) {
                    headerParameters["Authorization"] = `Bearer ${tokenString}`;
                }
            }
            const response = yield this.request({
                path: `/projects/{project_id}`.replace(`{${"project_id"}}`, encodeURIComponent(String(requestParameters.projectId))),
                method: 'GET',
                headers: headerParameters,
                query: queryParameters,
            });
            return new runtime.JSONApiResponse(response);
        });
    }
    /**
     * Retrieve a project
     */
    getProject(projectId) {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.getProjectRaw({ projectId: projectId });
            return yield response.value();
        });
    }
    /**
     * List a project\'s enrichments
     */
    getProjectEnrichmentsRaw(requestParameters) {
        return __awaiter(this, void 0, void 0, function* () {
            if (requestParameters.projectId === null || requestParameters.projectId === undefined) {
                throw new runtime.RequiredError('projectId', 'Required parameter requestParameters.projectId was null or undefined when calling getProjectEnrichments.');
            }
            const queryParameters = {};
            const headerParameters = {};
            if (this.configuration && this.configuration.accessToken) {
                const token = this.configuration.accessToken;
                const tokenString = yield token("bearer", []);
                if (tokenString) {
                    headerParameters["Authorization"] = `Bearer ${tokenString}`;
                }
            }
            const response = yield this.request({
                path: `/projects/{project_id}/enrichments`.replace(`{${"project_id"}}`, encodeURIComponent(String(requestParameters.projectId))),
                method: 'GET',
                headers: headerParameters,
                query: queryParameters,
            });
            return new runtime.JSONApiResponse(response);
        });
    }
    /**
     * List a project\'s enrichments
     */
    getProjectEnrichments(projectId) {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.getProjectEnrichmentsRaw({ projectId: projectId });
            return yield response.value();
        });
    }
    /**
     * Get every Project defined on the account.
     * List projects
     */
    getProjectsRaw(requestParameters) {
        return __awaiter(this, void 0, void 0, function* () {
            const queryParameters = {};
            if (requestParameters.ids) {
                queryParameters['ids'] = requestParameters.ids;
            }
            const headerParameters = {};
            if (this.configuration && this.configuration.accessToken) {
                const token = this.configuration.accessToken;
                const tokenString = yield token("bearer", []);
                if (tokenString) {
                    headerParameters["Authorization"] = `Bearer ${tokenString}`;
                }
            }
            const response = yield this.request({
                path: `/projects`,
                method: 'GET',
                headers: headerParameters,
                query: queryParameters,
            });
            return new runtime.JSONApiResponse(response);
        });
    }
    /**
     * Get every Project defined on the account.
     * List projects
     */
    getProjects(ids) {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.getProjectsRaw({ ids: ids });
            return yield response.value();
        });
    }
    /**
     * Read a Project\'s columns for people you name, synchronously.  The Project supplies the columns; this request supplies the rows. Results come back aligned to `subjects` by index, and each subject\'s payload holds one key per column in the Project and nothing else. One request carries at most 100 subjects.  Grouping never applies here, because a lookup has no group. Collapsing one person\'s several source values does apply, using each column\'s own strategy, as does imputation.  A `previous_*` or `*_delta` value is always null: those values come from an enrichment\'s own delivery history, and a lookup keeps no such history.  A request that would exceed your remaining quota is refused rather than answered with a payload of nulls, so refusal is never mistaken for absence.
     * Look up people in a project
     */
    lookupProjectRaw(requestParameters) {
        return __awaiter(this, void 0, void 0, function* () {
            if (requestParameters.projectId === null || requestParameters.projectId === undefined) {
                throw new runtime.RequiredError('projectId', 'Required parameter requestParameters.projectId was null or undefined when calling lookupProject.');
            }
            if (requestParameters.lookupRequest === null || requestParameters.lookupRequest === undefined) {
                throw new runtime.RequiredError('lookupRequest', 'Required parameter requestParameters.lookupRequest was null or undefined when calling lookupProject.');
            }
            const queryParameters = {};
            const headerParameters = {};
            headerParameters['Content-Type'] = 'application/json';
            if (this.configuration && this.configuration.accessToken) {
                const token = this.configuration.accessToken;
                const tokenString = yield token("bearer", []);
                if (tokenString) {
                    headerParameters["Authorization"] = `Bearer ${tokenString}`;
                }
            }
            const response = yield this.request({
                path: `/projects/{project_id}/lookup`.replace(`{${"project_id"}}`, encodeURIComponent(String(requestParameters.projectId))),
                method: 'POST',
                headers: headerParameters,
                query: queryParameters,
                body: requestParameters.lookupRequest,
            });
            return new runtime.JSONApiResponse(response);
        });
    }
    /**
     * Read a Project\'s columns for people you name, synchronously.  The Project supplies the columns; this request supplies the rows. Results come back aligned to `subjects` by index, and each subject\'s payload holds one key per column in the Project and nothing else. One request carries at most 100 subjects.  Grouping never applies here, because a lookup has no group. Collapsing one person\'s several source values does apply, using each column\'s own strategy, as does imputation.  A `previous_*` or `*_delta` value is always null: those values come from an enrichment\'s own delivery history, and a lookup keeps no such history.  A request that would exceed your remaining quota is refused rather than answered with a payload of nulls, so refusal is never mistaken for absence.
     * Look up people in a project
     */
    lookupProject(projectId, lookupRequest) {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.lookupProjectRaw({ projectId: projectId, lookupRequest: lookupRequest });
            return yield response.value();
        });
    }
    /**
     * See what this Project produces, without having to name anyone.  A few people are picked for you and read through the same path a lookup uses, so a sample shows exactly which columns a lookup can fill in and which come back null. Identity class values are blurred, because the people shown are real.  Creating or editing a Project never samples data. Sampling reads person data, so it creates usage under the lookup rule. The rows belong to this response and are not stored, so asking again shows different people and current values.
     * Sample a project
     */
    sampleProjectRaw(requestParameters) {
        return __awaiter(this, void 0, void 0, function* () {
            if (requestParameters.projectId === null || requestParameters.projectId === undefined) {
                throw new runtime.RequiredError('projectId', 'Required parameter requestParameters.projectId was null or undefined when calling sampleProject.');
            }
            if (requestParameters.sampleRequest === null || requestParameters.sampleRequest === undefined) {
                throw new runtime.RequiredError('sampleRequest', 'Required parameter requestParameters.sampleRequest was null or undefined when calling sampleProject.');
            }
            const queryParameters = {};
            const headerParameters = {};
            headerParameters['Content-Type'] = 'application/json';
            if (this.configuration && this.configuration.accessToken) {
                const token = this.configuration.accessToken;
                const tokenString = yield token("bearer", []);
                if (tokenString) {
                    headerParameters["Authorization"] = `Bearer ${tokenString}`;
                }
            }
            const response = yield this.request({
                path: `/projects/{project_id}/sample`.replace(`{${"project_id"}}`, encodeURIComponent(String(requestParameters.projectId))),
                method: 'POST',
                headers: headerParameters,
                query: queryParameters,
                body: requestParameters.sampleRequest,
            });
            return new runtime.JSONApiResponse(response);
        });
    }
    /**
     * See what this Project produces, without having to name anyone.  A few people are picked for you and read through the same path a lookup uses, so a sample shows exactly which columns a lookup can fill in and which come back null. Identity class values are blurred, because the people shown are real.  Creating or editing a Project never samples data. Sampling reads person data, so it creates usage under the lookup rule. The rows belong to this response and are not stored, so asking again shows different people and current values.
     * Sample a project
     */
    sampleProject(projectId, sampleRequest) {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.sampleProjectRaw({ projectId: projectId, sampleRequest: sampleRequest });
            return yield response.value();
        });
    }
    /**
     * Unarchive a project
     */
    unarchiveProjectRaw(requestParameters) {
        return __awaiter(this, void 0, void 0, function* () {
            if (requestParameters.projectId === null || requestParameters.projectId === undefined) {
                throw new runtime.RequiredError('projectId', 'Required parameter requestParameters.projectId was null or undefined when calling unarchiveProject.');
            }
            const queryParameters = {};
            const headerParameters = {};
            if (requestParameters.idempotencyKey !== undefined && requestParameters.idempotencyKey !== null) {
                headerParameters['Idempotency-Key'] = String(requestParameters.idempotencyKey);
            }
            if (this.configuration && this.configuration.accessToken) {
                const token = this.configuration.accessToken;
                const tokenString = yield token("bearer", []);
                if (tokenString) {
                    headerParameters["Authorization"] = `Bearer ${tokenString}`;
                }
            }
            const response = yield this.request({
                path: `/projects/{project_id}/unarchive`.replace(`{${"project_id"}}`, encodeURIComponent(String(requestParameters.projectId))),
                method: 'POST',
                headers: headerParameters,
                query: queryParameters,
            });
            return new runtime.JSONApiResponse(response);
        });
    }
    /**
     * Unarchive a project
     */
    unarchiveProject(projectId, idempotencyKey) {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.unarchiveProjectRaw({ projectId: projectId, idempotencyKey: idempotencyKey });
            return yield response.value();
        });
    }
    /**
     * Change a Project\'s columns or their order.  Changing what a column means while keeping its header makes that column\'s previous values and deltas meaningless, so an enrichment stops reporting them until the column\'s meaning settles again.
     * Update a project
     */
    updateProjectRaw(requestParameters) {
        return __awaiter(this, void 0, void 0, function* () {
            if (requestParameters.projectId === null || requestParameters.projectId === undefined) {
                throw new runtime.RequiredError('projectId', 'Required parameter requestParameters.projectId was null or undefined when calling updateProject.');
            }
            if (requestParameters.projectFields === null || requestParameters.projectFields === undefined) {
                throw new runtime.RequiredError('projectFields', 'Required parameter requestParameters.projectFields was null or undefined when calling updateProject.');
            }
            const queryParameters = {};
            const headerParameters = {};
            headerParameters['Content-Type'] = 'application/merge-patch+json';
            if (this.configuration && this.configuration.accessToken) {
                const token = this.configuration.accessToken;
                const tokenString = yield token("bearer", []);
                if (tokenString) {
                    headerParameters["Authorization"] = `Bearer ${tokenString}`;
                }
            }
            const response = yield this.request({
                path: `/projects/{project_id}`.replace(`{${"project_id"}}`, encodeURIComponent(String(requestParameters.projectId))),
                method: 'PATCH',
                headers: headerParameters,
                query: queryParameters,
                body: requestParameters.projectFields,
            });
            return new runtime.JSONApiResponse(response);
        });
    }
    /**
     * Change a Project\'s columns or their order.  Changing what a column means while keeping its header makes that column\'s previous values and deltas meaningless, so an enrichment stops reporting them until the column\'s meaning settles again.
     * Update a project
     */
    updateProject(projectId, projectFields) {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.updateProjectRaw({ projectId: projectId, projectFields: projectFields });
            return yield response.value();
        });
    }
}
exports.ProjectsApi = ProjectsApi;
