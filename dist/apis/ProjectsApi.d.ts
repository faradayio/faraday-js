/**
 * Faraday REST API
 * The [Faraday](https://faraday.ai) API makes it easy to predict customer behavior programmatically. See our [API change policy](https://faraday.ai/legal).
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@faraday.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
import * as runtime from '../runtime';
import { Enrichment, Project, ProjectLookupRequest, ProjectLookupResponse, ProjectMergePatch, ProjectPost, ProjectSampleRequest } from '../models';
export interface ArchiveProjectRequest {
    projectId: string;
    idempotencyKey?: string;
}
export interface CreateProjectRequest {
    project: ProjectPost;
    idempotencyKey?: string;
}
export interface DeleteProjectRequest {
    projectId: string;
}
export interface GetProjectRequest {
    projectId: string;
}
export interface GetProjectEnrichmentsRequest {
    projectId: string;
}
export interface GetProjectsRequest {
    ids?: Array<string>;
}
export interface LookupProjectRequest {
    projectId: string;
    lookupRequest: ProjectLookupRequest;
}
export interface SampleProjectRequest {
    projectId: string;
    sampleRequest: ProjectSampleRequest;
}
export interface UnarchiveProjectRequest {
    projectId: string;
    idempotencyKey?: string;
}
export interface UpdateProjectRequest {
    projectId: string;
    projectFields: ProjectMergePatch;
}
/**
 *
 */
export declare class ProjectsApi extends runtime.BaseAPI {
    /**
     * Archive a Project. A Project that a live Enrichment still refers to cannot be archived; the error names the Enrichments in the way.
     * Archive a project
     */
    archiveProjectRaw(requestParameters: ArchiveProjectRequest): Promise<runtime.ApiResponse<Project>>;
    /**
     * Archive a Project. A Project that a live Enrichment still refers to cannot be archived; the error names the Enrichments in the way.
     * Archive a project
     */
    archiveProject(projectId: string, idempotencyKey?: string): Promise<Project>;
    /**
     * Declare the exact output columns you want, in the exact order you want them.  The response carries the resolved output schema and the usage ceiling straight away. No data is read and no person is sampled.
     * Create a project
     */
    createProjectRaw(requestParameters: CreateProjectRequest): Promise<runtime.ApiResponse<Project>>;
    /**
     * Declare the exact output columns you want, in the exact order you want them.  The response carries the resolved output schema and the usage ceiling straight away. No data is read and no person is sampled.
     * Create a project
     */
    createProject(project: ProjectPost, idempotencyKey?: string): Promise<Project>;
    /**
     * Delete a Project. A Project that a live Enrichment still refers to cannot be deleted; the error names the Enrichments in the way. Archive or delete those first.
     * Delete a project
     */
    deleteProjectRaw(requestParameters: DeleteProjectRequest): Promise<runtime.ApiResponse<void>>;
    /**
     * Delete a Project. A Project that a live Enrichment still refers to cannot be deleted; the error names the Enrichments in the way. Archive or delete those first.
     * Delete a project
     */
    deleteProject(projectId: string): Promise<void>;
    /**
     * Retrieve a project
     */
    getProjectRaw(requestParameters: GetProjectRequest): Promise<runtime.ApiResponse<Project>>;
    /**
     * Retrieve a project
     */
    getProject(projectId: string): Promise<Project>;
    /**
     * List a project\'s enrichments
     */
    getProjectEnrichmentsRaw(requestParameters: GetProjectEnrichmentsRequest): Promise<runtime.ApiResponse<Array<Enrichment>>>;
    /**
     * List a project\'s enrichments
     */
    getProjectEnrichments(projectId: string): Promise<Array<Enrichment>>;
    /**
     * Get every Project defined on the account.
     * List projects
     */
    getProjectsRaw(requestParameters: GetProjectsRequest): Promise<runtime.ApiResponse<Array<Project>>>;
    /**
     * Get every Project defined on the account.
     * List projects
     */
    getProjects(ids?: Array<string>): Promise<Array<Project>>;
    /**
     * Read a Project\'s columns for people you name, synchronously.  The Project supplies the columns; this request supplies the rows. Results come back aligned to `subjects` by index, and each subject\'s payload holds one key per column in the Project and nothing else. One request carries at most 100 subjects.  Grouping never applies here, because a lookup has no group. Collapsing one person\'s several source values does apply, using each column\'s own strategy, as does imputation.  A `previous_*` or `*_delta` value is always null: those values come from an enrichment\'s own delivery history, and a lookup keeps no such history.  A request that would exceed your remaining quota is refused rather than answered with a payload of nulls, so refusal is never mistaken for absence.
     * Look up people in a project
     */
    lookupProjectRaw(requestParameters: LookupProjectRequest): Promise<runtime.ApiResponse<ProjectLookupResponse>>;
    /**
     * Read a Project\'s columns for people you name, synchronously.  The Project supplies the columns; this request supplies the rows. Results come back aligned to `subjects` by index, and each subject\'s payload holds one key per column in the Project and nothing else. One request carries at most 100 subjects.  Grouping never applies here, because a lookup has no group. Collapsing one person\'s several source values does apply, using each column\'s own strategy, as does imputation.  A `previous_*` or `*_delta` value is always null: those values come from an enrichment\'s own delivery history, and a lookup keeps no such history.  A request that would exceed your remaining quota is refused rather than answered with a payload of nulls, so refusal is never mistaken for absence.
     * Look up people in a project
     */
    lookupProject(projectId: string, lookupRequest: ProjectLookupRequest): Promise<ProjectLookupResponse>;
    /**
     * See what this Project produces, without having to name anyone.  A few people are picked for you and read through the same path a lookup uses, so a sample shows exactly which columns a lookup can fill in and which come back null. Identity class values are blurred, because the people shown are real.  Creating or editing a Project never samples data. Sampling reads person data, so it creates usage under the lookup rule. The rows belong to this response and are not stored, so asking again shows different people and current values.
     * Sample a project
     */
    sampleProjectRaw(requestParameters: SampleProjectRequest): Promise<runtime.ApiResponse<Project>>;
    /**
     * See what this Project produces, without having to name anyone.  A few people are picked for you and read through the same path a lookup uses, so a sample shows exactly which columns a lookup can fill in and which come back null. Identity class values are blurred, because the people shown are real.  Creating or editing a Project never samples data. Sampling reads person data, so it creates usage under the lookup rule. The rows belong to this response and are not stored, so asking again shows different people and current values.
     * Sample a project
     */
    sampleProject(projectId: string, sampleRequest: ProjectSampleRequest): Promise<Project>;
    /**
     * Unarchive a project
     */
    unarchiveProjectRaw(requestParameters: UnarchiveProjectRequest): Promise<runtime.ApiResponse<Project>>;
    /**
     * Unarchive a project
     */
    unarchiveProject(projectId: string, idempotencyKey?: string): Promise<Project>;
    /**
     * Change a Project\'s columns or their order.  Changing what a column means while keeping its header makes that column\'s previous values and deltas meaningless, so an enrichment stops reporting them until the column\'s meaning settles again.
     * Update a project
     */
    updateProjectRaw(requestParameters: UpdateProjectRequest): Promise<runtime.ApiResponse<Project>>;
    /**
     * Change a Project\'s columns or their order.  Changing what a column means while keeping its header makes that column\'s previous values and deltas meaningless, so an enrichment stops reporting them until the column\'s meaning settles again.
     * Update a project
     */
    updateProject(projectId: string, projectFields: ProjectMergePatch): Promise<Project>;
}
